envSecrets:
  MONGODB_URI: inventory-management

# Kanopy security and ownership (requires web-app chart v4.30.0+)
security:
  dataClassification: "Internal"
  dataDesignation: ["MI"]

ownership:
  driEmail: rami.pinto@mongodb.com
